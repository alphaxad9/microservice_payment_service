apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.kafka }}
  labels:
    app: kafka
    app.kubernetes.io/component: message-broker
type: Opaque
stringData:
  KAFKA_CLIENT_ID: "payment-service"
  # Add SASL credentials here if you enable authentication later
  # KAFKA_SASL_USERNAME: {{ .Values.config.payment.kafkaUser | quote }}
  # KAFKA_SASL_PASSWORD: {{ .Values.config.payment.kafkaPass | quote }}